"""V3.6 A/B Validation Tests — chronological split + frozen Concept B + OOS separation."""
# Positive/negative examples for A (current) and B_strict/B_moderate/B_permissive.
# Tests verify: no lookahead, SV excluded, 3-12 bar window, congestion boundaries, state transitions, chronological split preserved, no parameter tuning on OOS.
# No statistical significance claim; sample expanded but single dataset.
